
// import { create } from 'zustand';

// interface SwitchModeState {
//   mode: string;
//   setMode: (mode: string) => void;
// }

// const useSwitchMode = create<SwitchModeState>((set) => ({
//   mode: 'Upload', 
//   setMode: (mode: string) => set({ mode }),
// }));

// export default useSwitchMode;